export type Mood = {
  id: string;
  emoji: string;
  name: string;
  colors: string[];
  animation: string;
};

export type Playlist = {
  title: string;
  description: string;
  tracks: string[];
};